<Window x:Class="SkyCastRealtime.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SkyCastRealtime"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Summary" Command="{Binding NavigateCommand}" CommandParameter="WeatherSummary" />
            <MenuItem Header="Detail" Command="{Binding NavigateCommand}" CommandParameter="WeatherDetail" />
            <MenuItem Header="Alert" Command="{Binding NavigateCommand}" CommandParameter="WeatherAlert" />
        </Menu>
        <ContentControl Content="{Binding CurrentViewModel}" />
    </DockPanel>
</Window>
